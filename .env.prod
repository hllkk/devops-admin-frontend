# backend service base url, prod environment
# Docker 部署使用相对路径，由 nginx 反向代理 /api 到后端
VITE_SERVICE_BASE_URL=

# other backend service base url, prod environment
VITE_OTHER_SERVICE_BASE_URL= `{
  "demo": "http://localhost:9529"
}`
